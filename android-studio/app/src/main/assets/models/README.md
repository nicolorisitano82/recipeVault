Inserisci qui eventuali modelli `.task` per il provider locale Android.

Esempio di percorso da usare nell'app:

`asset://models/gemma-3n-e2b.task`

Nota: i modelli sono troppo grandi per essere inclusi nel repository, quindi questa cartella resta solo come punto di appoggio.
